{
  "name": "@budget/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Moteur de calcul budgétaire — pur, testable, partagé entre le client offline et l'API serveur.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --experimental-strip-types --test tests/*.test.ts",
    "rapport": "node --experimental-strip-types scripts/rapport.ts"
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
